id = "process-icon"
name = "Process Icon (flowchart node)"
description = "A single clean icon for one flowchart/process step — centred object on transparent/white, made to sit inside a node box."
when_to_use = "Generating the per-step icons of an illustrated flowchart (the figurelabs 'icons not just boxes' look). Call once per step; reuse the figure's palette so every node matches."
tags = ["icon", "flowchart", "process", "step", "flat"]
aspect = "1:1"
resolution = "1K"
provider = "gemini"
model = "gemini/gemini-3-pro-image"
palette = ["#2C6E9B", "#F0B429", "#DE5B49", "#4FA373", "#6B7280", "#FFFFFF"]
palette_locked = false
negative = "no text, no labels, no number badges, no drop shadow, no photographic realism, no background scene, no frame or border"
prompt = """
A single clean flat icon representing {subject}. One clear, centred object filling most of the frame,
confident uniform outlines and flat fills with at most one subtle shade, on a plain white background
(no border, no scenery). Simple, instantly readable at small size, consistent line weight — an icon
that will sit inside a flowchart node box alongside others of the same set.
"""
exemplars = []
