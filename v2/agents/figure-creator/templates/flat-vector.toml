id = "flat-vector"
name = "Flat Vector Schematic"
description = "Clean flat-vector illustration: uniform outlines, flat fills, limited palette. The crisp schematic look."
when_to_use = "When a deliberately flat, minimal, schematic look is wanted — icons, simple mechanisms, slide graphics, or figures that will be traced to editable shapes."
tags = ["flat", "vector", "schematic", "minimal", "icon"]
aspect = "1:1"
resolution = "2K"
provider = "gemini"
model = "gemini/gemini-3-pro-image"
palette = ["#2C6E9B", "#F0B429", "#DE5B49", "#4FA373", "#6B7280", "#F7F5F0"]
palette_locked = false
negative = "no photorealism, no gradients toward realism, no ambient occlusion, no drop shadows, no texture or grain, no background scenery, no baked-in text"
prompt = """
A clean FLAT-VECTOR scientific illustration of {subject}. Bold, well-defined shapes with confident
UNIFORM outlines and flat fills (at most one subtle directional shade per shape — no realistic
modelling). Limited, harmonious solid-colour palette; distinct, cleanly separable parts; generous
whitespace on a pure white background. The whole subject fits inside the frame with a margin.
Minimal, modern, editorial — the look of a top-tier vector infographic.
"""
exemplars = []
