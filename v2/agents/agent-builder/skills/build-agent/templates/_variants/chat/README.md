# chat — the default template

The base skeleton IS the chat app, so this overlay carries exactly one file: `src/theme.css`,
the template's own palette (light, indigo — a messenger). Everything else comes from the base.

A variant folder holds only what differs from the base; the folders in `_variants/` are the whole
answer to "which templates are there?".
