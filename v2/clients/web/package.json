{
  "name": "agentd-web",
  "private": true,
  "version": "0.1.6",
  "description": "agentd hosted web client — the SAME renderer the desktop shell runs, served as a static site by nginx.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@agentd/ui": "*",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.7.2",
    "vite": "^6.0.5"
  }
}
