id = "vision"
name = "Vision"
kind = "native"
entry = "vision_plugin:register"

# Image-analysis through the runtime's model funnel (oneshot) — mode-correct: BYOK uses
# GEMINI_API_KEY/GOOGLE_API_KEY, cloud modes ride the platform's metered proxy. No key held here.
# No [requires] env gate: tools error gracefully at call time if no route to a model exists.
