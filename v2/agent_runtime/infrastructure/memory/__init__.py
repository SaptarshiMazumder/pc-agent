"""Memory implementations (the MemoryRepository interface): where conversation
history + memory is stored. local_store.py is the local file backend (now); a
cloud, end-to-end-encrypted bank is a future sibling here."""
