litellm>=1.60
websockets>=13
rich>=13
httpx>=0.27
aiohttp>=3.10     # webhook ingress server for push channels (LINE, Phase 5b/L2)
trafilatura>=1.12
readability-lxml>=0.8
markdownify>=0.13
ddgs>=2.0
playwright>=1.49
jsonschema>=4.21
croniter>=2.0     # cron-expression scheduling (day-of-week, etc.) for the cron tool
python-docx>=1.1
pypdf>=4.0
openpyxl>=3.1
python-pptx>=1.0
pytest>=8
pytest-asyncio>=0.24
import-linter>=2.0
# computer-use tool (optional; only needed when AGENTD_COMPUTER_ENABLED=1).
# The tool degrades to "disabled" if these are missing.
pyautogui>=0.9.54
Pillow>=10
google-genai>=0.3
# MCP client (optional; only needed when mcp_servers are configured).
# MCP degrades to "disabled" if this is missing.
mcp>=1.0
