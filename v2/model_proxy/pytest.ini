# This service is packaged independently of agentd, so it owns its pytest config too.
# Its presence also makes pytest pick THIS as rootdir when the tests are invoked from v2/,
# keeping a local run identical to the CI job.
[pytest]
testpaths = tests
asyncio_mode = strict
asyncio_default_fixture_loop_scope = function
