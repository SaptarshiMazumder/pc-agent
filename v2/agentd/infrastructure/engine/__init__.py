"""Agent engine implementations (the AgentEngine interface): the reason->act loop.
native.py is our hand-rolled loop (default). Claude SDK / LangGraph engines would be
siblings here, each implementing the same AgentEngine interface."""
