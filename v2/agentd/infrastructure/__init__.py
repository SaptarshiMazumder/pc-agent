"""Infrastructure layer: concrete implementations of the application interfaces
(LLM client, tools, memory store, engine, transport). Everything that touches an
external library, the network, the disk, or a subprocess lives here."""
