/* Thinking — the animated working indicator. */
import { Thinking } from 'agent-app'

export const Default = () => <Thinking />

export const NamedStep = () => <Thinking label="Reading the instance" />
