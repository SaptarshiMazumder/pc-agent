/* Credits — the billing page. With no accounts service reachable it renders its no-service
 * state, which is what a static page can truthfully show. */
import { Credits } from 'agent-app'

export const Page = () => <Credits agentId="comfy-artchitect" />
